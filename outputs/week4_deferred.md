# Week 4 Deferred Items

- PettingZoo multi-agent smoke test is implemented at `src/week4_smoketest.py`, but it was not run because `SUMO_HOME` is unavailable.
- RLlib independent multi-agent training was not implemented or executed because adding `ray[rllib]` requires approval and long training.
- PyTorch Geometric graph and GAT scaffold files were added, but runtime verification requires `torch_geometric`.
- Three 500,000-step MARL checkpoints were not produced.

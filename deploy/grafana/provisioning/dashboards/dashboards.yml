apiVersion: 1

providers:
  - name: SmartFlow
    orgId: 1
    folder: SmartFlow
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
